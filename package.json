{
  "name": "theentirerobot.com",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export && shopt -s dotglob && cp -R out-files/ out/",
    "deploy": "gh-pages -d out",
    "build-deploy": "yarn build && yarn export && yarn deploy"
  },
  "dependencies": {
    "next": "^7.0.2",
    "react": "^16.7.0",
    "react-dom": "^16.7.0"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
